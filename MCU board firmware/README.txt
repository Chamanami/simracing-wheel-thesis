The MCU board (STM32/ESP32) handles the real-time reading of sendors, button presses,
rotary encoders, and controls LEDs. It communicates these inputs to the PC over USB,
and receives configuration commands from the GUI through the drivers.
